# Slidev Course Audit Log

Tracks every change made to presentations during the full-course audit. Each deck has a fixed `###` stub below — append bullets under your own deck's stub only, newest last. Do not create new stubs or reorder sections; this keeps parallel edits conflict-free.

Bullet format: `- YYYY-MM-DD [scaffold|content|notes|verify] <what changed> — <why>`

## Broader Context

### Ethical Considerations and Failure Modes

- 2026-07-27 [content] Expanded the deck from 7 to 10 slides: added an "ERM assumption" framing slide connecting the empirical-risk-minimization objective from Core ML Concepts to why distribution shift/feedback loops/adversarial inputs/dual-use all matter, and added two real-world case-study slides (Zillow's iBuying pricing algorithm for distribution shift; Lum & Isaac's 2016 predictive-policing audit for feedback-loop amplification) — why: the original deck named failure modes (distribution shift, feedback loops) abstractly with no worked example a student could check against, and asserted "the model keeps returning confident predictions" without ever explaining softmax's normalization behavior.
- 2026-07-27 [content] Added a softmax equation card to "Confidently Wrong at the Edges" explaining precisely why there is no built-in "none of the above" class, and named the specific 2018 Eykholt et al. adversarial stop-sign study rather than describing adversarial examples generically — why: the slide asserted the phenomenon without any mechanism or citation a student could verify or look up.
- 2026-07-27 [notes] Rewrote every speaker-note block from one-liners (~10-15 words) to self-contained textbook-depth prose (200-500+ words each), defining every term used (covariate vs. concept shift, i.i.d. assumption, OOD), explaining why each design choice/example was chosen, and expanding terse callbacks (e.g. "Cross-validation ... cannot detect a multi-cycle feedback loop") into full explanations of the underlying mechanism, plus an explicit transition to the next deck (Fairness, Bias, and Interpretability) — why: original notes were too terse for a student to learn the material from the deck alone, per the module's quality bar.
- 2026-07-27 [verify] Playwright audit (`scripts/audit_slidev.js`, port 3140, session `bc-audit`) passed on first run after content changes: 10/10 slides, zero overflow/katex-error problems, click-reveal confirmed working, zero console errors — `{"passed":true}`. `npm run build` and `npm run export` succeeded; `slides-export.pdf` has 10 pages, matching the 10 slides in `slides.md`.

### Fairness, Bias, and Interpretability

- 2026-07-27 [content] Expanded the deck from 8 to 9 slides, adding a full case-study slide on the 2016 ProPublica/Northpointe COMPAS recidivism-scoring controversy (concrete numbers and the calibration-vs-error-rate dispute) placed directly before the formal group-fairness-definitions slide, and added exact mathematical definitions (conditional-probability notation) for demographic parity, equalized odds, and predictive parity, plus a fully worked numeric SHAP waterfall example (baseline 0.30 default probability → income/debt/history contributions → final 0.52) replacing the generic unlabeled "conceptual chart" placeholder — why: the original deck asserted fairness-metric names and the impossibility result with no case a student could check against, and the SHAP slide's SVG had no real numbers behind it.
- 2026-07-27 [notes] Rewrote every speaker-note block into self-contained, textbook-depth prose: defined every symbol in the fairness-metric formulas, explained the Chouldechova/Kleinberg-Mullainathan-Raghavan impossibility result mechanism (not just "not an engineering bug"), explained the Shapley-value game-theoretic derivation behind SHAP rather than asserting the additive property, and expanded terse callbacks (e.g. "ERM has no intrinsic fairness objective") into full explanations with explicit transitions between slides and into the next deck — why: original notes (~10-13 words/block) could not teach the content standalone.
- 2026-07-27 [verify] Playwright audit (`scripts/audit_slidev.js`, port 3140, session `bc-audit`) passed on first run: 9/9 slides, zero overflow/katex-error problems, click-reveal confirmed working, zero console errors — `{"passed":true}`. `npm run build` and `npm run export` succeeded; `slides-export.pdf` has 9 pages, matching the 9 slides in `slides.md`.

### Where Classical ML Ends and Modern Deep Learning Begins

- 2026-07-27 [content] Kept the deck at 7 slides but substantially deepened each one: added a logistic-regression-as-zero-hidden-layer-network equation to "The Boundary Is Fuzzy" to make the "convention, not theorem" claim concrete and checkable; added a worked example (5,000-row loan-application dataset with a regulatory explanation requirement) to "A Practical Choice Map" showing the four-step framework applied end-to-end; relabeled the generic "conceptual chart" SVG on the scaling slide with an actual classical-plateaus-vs-deep-keeps-improving legend and accessible aria-label — why: the original deck asserted "the boundary is fuzzy" and "classical ML is often better" as bare claims with no example connecting them to material from earlier in the course (bias-variance tradeoff, ERM).
- 2026-07-27 [notes] Rewrote every speaker-note block into self-contained, textbook-depth prose, explicitly tying each slide back to earlier course units (bias-variance tradeoff from Core ML Concepts for the small-data slide, backpropagation-as-chain-rule from Calculus for Optimization on the foundations slide, SHAP/interpretability from the Fairness deck on the interpretability-constraint slide) and adding an explicit transition to the capstone project — why: original notes were one-line assertions with no mechanism or connective tissue for a student working from the deck alone.
- 2026-07-27 [verify] Playwright audit (`scripts/audit_slidev.js`, port 3140, session `bc-audit`) initially failed with two bottom-edge canvas overflows (22px and 48px) on the "Where Deep Learning Changed the Landscape" and "The Mathematical Foundations Do Not Change" slides after notes/content expansion added a 4th stacked card; fixed by tightening card padding/spacing and trimming card text, then reran and got `{"passed":true}` — 7/7 slides, zero overflow/katex-error problems, click-reveal confirmed working, zero console errors. `npm run build` and `npm run export` succeeded; `slides-export.pdf` has 7 pages, matching the 7 slides in `slides.md`.

## Capstone - Project Time

### End-to-End Project Guide

- 2026-07-27 [content] Expanded the deck from 10 to 22 slides, adding a pipeline-roadmap slide, a running worked example ("Meet the Worked Example: Subscription Churn"), and new stage-detail slides: Handling Missing Data (MCAR/MAR/MNAR + 4 imputation strategies), Outliers/Duplicates/Leakage, Encoding and Feature Engineering, Choosing Candidate Models, Tune Hyperparameters Broad-to-Narrow, Reading the Confusion Matrix, Is the Difference Real? (statistical significance), Robustness Checks, Fairness and Broader Consequences, and Common Mistakes at Each Stage — why: the original deck asserted pipeline steps ("handle missing data," "tune promising candidates") without explaining the actual techniques or tradeoffs, and had no worked example connecting stages.
- 2026-07-27 [content] Replaced the placeholder "Column A/Column B, Row A/Row B, ✓/×" confusion matrix on the evaluation slide with an actual Predicted Churn/Retain × Actual Churn/Retain matrix using real, internally-consistent counts (TP=96, FP=54, FN=64, TN=786 on 1,000 test customers) and derived precision/recall/F1/accuracy from those counts by hand, matching the numbers cited on the model-comparison and significance slides — why: the generic matrix taught nothing and duplicated a full Confusion Matrices deck elsewhere in the course; the numbers now trace consistently across three slides instead of being asserted independently.
- 2026-07-27 [content] Named specific earlier-module lectures throughout instead of generic phrases: Train-Validation-Test Splits and Cross-Validation, Overfitting and Regularization, Bias-Variance Tradeoff, Loss Functions and Empirical Risk Minimization, Accuracy/Precision/Recall/F1/ROC-AUC, Confusion Matrices, Common Pitfalls - Data Leakage and Class Imbalance, Statistical Significance in Model Comparison, the Supervised Learning and Ensemble Methods decks, Hyperparameter Tuning Strategies, Gradient Descent Variants, and Fairness/Bias/Interpretability and Ethical Considerations from Broader Context — why: this is the course's synthesis lecture and should point back to the specific prior lectures each stage draws on, not just say "earlier units."
- 2026-07-27 [notes] Rewrote all 22 speaker-note blocks to textbook-depth prose (~46-90 words/block, up from ~10): every term is defined on first use (MCAR/MAR/MNAR, one-hot vs. ordinal encoding, paired t-test, SHAP, distribution shift), each recommendation is justified, common student mistakes are flagged explicitly per stage, and every slide ends with an explicit transition to the next. — why: original notes were one-sentence assertions a student could not learn from without already knowing the material.
- 2026-07-27 [verify] Fixed a code-block/card overflow on the Stage 2b (Encoding and Feature Engineering) slide by tightening card padding/text size and shortening the ColumnTransformer snippet with click-based highlighting; re-ran `scripts/audit_slidev.js` via playwright-cli (`-s=cap-audit`, 1920x1080, `?clicks=99`) until it returned `"passed":true` for all 22 slides (zero overflow/KaTeX/console errors, click-reveal confirmed). Ran `npm run build` and `npm run export`; `slides-export.pdf` has 22 pages, matching the 22 slides.

## Core ML Concepts

### Bias-Variance Tradeoff

- 2026-07-27 [content] Inserted new derivation slide "Squaring: Why the Cross Terms Vanish" between the add-zero decomposition slide and the final identity slide, showing the $(b+v+\epsilon)^2$ expansion and why each cross term's expectation is zero — the decomposition previously jumped straight from the three-term split to the final identity without this algebraic step. — the identity was asserted, not derived; students need to see it is provable, not a metaphor.
- 2026-07-27 [content] Added a worked numeric example to the "Estimating Bias and Variance by Repeated Fitting" slide: ran the exact simulation code at polynomial degrees 1/3/10 and reported real bias²/variance/total numbers (e.g. degree 1: bias²≈0.220, variance≈0.013; degree 10: bias²≈0.00004, variance≈0.022) showing the numeric U-shape. — the concept was previously illustrated only abstractly via code with no concrete output.
- 2026-07-27 [notes] Rewrote all 11 speaker-note blocks to textbook depth: every symbol in the formal setup ($y$, $f(x)$, $\epsilon$, $\hat f$, $x_0$, the boxed expectation) is now defined explicitly; added an explicit overfitting-vs-high-variance / underfitting-vs-high-bias misconception callout on the dartboard slide, and a train/validation-vs-test-set framing note tying the diagnostic slide to the next deck; added explicit slide-to-slide transitions throughout. — previous notes were ~26-33 words/block and assumed familiarity with terms they never defined.
- 2026-07-27 [verify] `slidev` dev server on port 3142, playwright-cli session `core-audit`: `audit_slidev.js` result `{"total":11,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced `slides-export.pdf` with 11 pages, matching the 11-slide deck.

### Loss Functions and Empirical Risk Minimization

- 2026-07-27 [content] Inserted new derivation slide "Where Log Loss Comes From: Bernoulli Likelihood" before the cross-entropy numeric slide, deriving $\ell_{log}$ from the Bernoulli PMF via negative log-likelihood ($\mathcal L(\theta)=\prod \hat p_i^{y_i}(1-\hat p_i)^{1-y_i}$ → $-\log\mathcal L$ → averaged). — the deck previously asserted "this follows directly from negative log likelihood" without showing the derivation; also ties squared-error/Gaussian and log-loss/Bernoulli together explicitly.
- 2026-07-27 [notes] Rewrote all 12 speaker-note blocks to textbook depth: defined every symbol in the true-risk/empirical-risk pair, walked the outlier example's arithmetic (2025 vs 45, with percentages), explained the 0-1 loss gradient problem mechanically, and explicitly flagged the notational overload of $R(\theta)$ meaning "true risk" in one slide and "regularization penalty" in the final-objective slide. Added explicit cross-deck transitions tying this closing deck back to all four earlier Core ML Concepts decks. — previous notes were ~26-33 words/block.
- 2026-07-27 [verify] `slidev` dev server on port 3142, playwright-cli session `core-audit`: `audit_slidev.js` result `{"total":12,"problems":[],"clickReveal":{"slide":2,"before":3,"after":2,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced `slides-export.pdf` with 12 pages, matching the 12-slide deck (11 original + 1 new derivation slide).

### Overfitting and Regularization

- 2026-07-27 [content] Inserted new derivation slide "Deriving L2 and L1 from Priors" between the Bayesian-view slide and the Ridge slide, showing the actual algebra from Gaussian/Laplace prior densities to $-\log p(\theta)\propto\|\theta\|_2^2$ and $\propto\|\theta\|_1$ respectively. — the deck previously asserted "Gaussian prior ↔ L2 penalty" and "Laplace prior ↔ L1 penalty" as bare claims with no derivation.
- 2026-07-27 [notes] Rewrote all 12 speaker-note blocks to textbook depth: explained the mechanism behind every overfitting symptom (large/finely-balanced coefficients), walked the RidgeCV/log-spaced-grid code line by line, explained the Lagrangian-duality constrained-region picture behind the L1-diamond/L2-circle geometry slide, and added an explicit warning against tuning λ on the test set. Flagged the notational overload of $R(\theta)$ (true risk vs. complexity penalty) across this deck and the Loss Functions deck. — previous notes were ~26-33 words/block.
- 2026-07-27 [verify] `slidev` dev server on port 3142, playwright-cli session `core-audit`: `audit_slidev.js` result `{"total":12,"problems":[],"clickReveal":{"slide":2,"before":6,"after":5,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced `slides-export.pdf` with 12 pages, matching the 12-slide deck (11 original + 1 new derivation slide).

### Train-Validation-Test Splits and Cross-Validation

- 2026-07-28 [notes] Rewrote 11 of 13 speaker-note blocks (slides 3-13; the title slide and "Why Training Error Is Not Enough" were already textbook-depth) from one-to-three-sentence fragments (~15-40 words) to textbook-depth prose (roughly 150-350 words/block): explains why the memorization example's MSE is exactly zero rather than just small, walks the two-call `train_test_split` arithmetic that causes the most common validation-ratio mistake, explains why `StandardScaler` fit before splitting leaks test information even though the model never sees test labels, and names each of stratified/grouped/time-aware splitting's specific failure mode. Every block ends with an explicit transition to the next slide. — why: content/derivations were already sound but notes could not teach the material standalone at the depth of the module's other decks.
- 2026-07-28 [content] Tightened spacing (grid gap, card padding, worked-example margins) on "Five-Fold Cross-Validation" to fix a bottom-edge overflow (worked-example text and closing caption rendering below the visible canvas) that was present before this pass's edits — pre-existing content bug, not introduced by the notes rewrite.
- 2026-07-28 [verify] `slidev` dev server port 3160, playwright-cli session `core2-audit`: `audit_slidev.js` final result `{"total":13,"problems":[],"clickReveal":{"slide":2,"before":6,"after":5,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 5.5MB `slides-export.pdf`.

### Types of Learning - Supervised, Unsupervised, Reinforcement

- 2026-07-28 [notes] Rewrote all 10 speaker-note blocks from one-to-two-sentence fragments (~15-40 words) to textbook-depth prose (roughly 150-350 words/block): states Mitchell's formal E-T-P definition of learning and ties it to the ERM framework from the Loss Functions deck; walks the k-means code example line by line and explains why cluster IDs carry no intrinsic meaning; explains the credit-assignment problem behind reinforcement learning's delayed-reward feedback; distinguishes semi-supervised from self-supervised learning precisely (a common point of confusion). Every block ends with an explicit transition, and the closing slide's transition now explicitly bridges into the Bias-Variance Tradeoff deck's opening question. — why: content/structure were already sound but notes were one-line fragments that could not teach the taxonomy standalone.
- 2026-07-28 [verify] `slidev` dev server port 3160, playwright-cli session `core2-audit`: `audit_slidev.js` result `{"total":10,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}` on first run after notes rewrite (no content changes needed). `npm run build` succeeded; `npm run export` produced a 4.1MB `slides-export.pdf`.

## Ensemble Methods

- 2026-07-27 [scaffold] Confirmed all three decks (Bagging and Random Forests, Boosting (AdaBoost, Gradient Boosting), Why Ensembles Work) have structured-data entries in `scripts/generate_missing_presentations.mjs`. The hand-edited `slides.md` files are now authoritative for this module; the generator must NOT be rerun for these three decks going forward, since doing so would silently overwrite this pass's content and notes.

### Bagging and Random Forests

- 2026-07-27 [content] Inserted new derivation slide "Why Averaging Reduces Variance" (glowSeed 613) deriving Var(mean of B iid trees) = σ²/B from the covariance expansion of Var(sum), plus a worked numeric example (σ²=4, B=25 → variance 0.16), and explicitly flagged the independence assumption as the setup the "Why Ensembles Work" deck's ρσ² formula later relaxes. — the deck asserted "variance falls while bias changes little" without ever deriving it; this is the algebra AdaBoost's alpha-derivation sibling slide in the Boosting deck also required.
- 2026-07-27 [content] Replaced the "Random Forests Add Feature Randomness" SVG, which was a mislabeled neural-network diagram (nodes captioned "input/hidden/diverse/combine/output") that did not depict a forest at all, with an SVG of three small decision trees grown on different random feature subsets converging into a "Majority vote / average" box — matches the slide's caption and narrative. — a passing `audit_slidev.js` run does not catch a diagram depicting the wrong concept; this was flagged in an advisor review before the edit pass began.
- 2026-07-27 [content] Replaced the "Feature Importance" SVG, which reused the same generic S-curve line chart (labeled "primary signal"/"comparison") as two other decks' unrelated slides, with a bar chart of decreasing impurity-based importance values (f1=0.34 down to f5=0.10, summing to 1.0) matching the slide's actual topic. — same category of fix as the random-forest diagram: caption and visual now agree.
- 2026-07-27 [content] Added a worked out-of-bag numeric example to the Bootstrap Sampling slide (n=1,000 rows → ≈368 OOB rows per tree) and an explicit OOB-tree-count worked example to the Out-of-Bag Evaluation speaker notes (n_estimators=200 → ≈74 OOB trees per row). — "add worked numeric examples" was explicit in the task brief and the original deck had none.
- 2026-07-27 [notes] Rewrote all 9 speaker-note blocks (8 original + 1 new slide) to textbook depth (roughly 150-350 words/block, up from ~11-13): defines every term on first use (bootstrap sample, out-of-bag row, weak/strong learner contrast, impurity reduction, permutation importance), derives the 1/e out-of-bag fraction and the variance-of-the-average formula in full rather than asserting them, explicitly flags the "bagging reduces variance, not bias" misconception as the most commonly missed point in the deck, and gives an explicit transition into the Boosting deck on the closing slide.
- 2026-07-27 [verify] `slidev` dev server on port 3143, playwright-cli session `ens-audit`: `audit_slidev.js` result `{"total":9,"problems":[],"clickReveal":{"slide":2,"before":5,"after":4,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced `slides-export.pdf` with 9 pages, matching the 9-slide deck (was 8 slides/pages before this pass).

### Boosting (AdaBoost, Gradient Boosting)

- 2026-07-27 [content] Inserted new derivation slide "Deriving AdaBoost's Alpha" (glowSeed 642) showing the full derivation from weighted exponential loss $L_t(\alpha)=(1-\epsilon_t)e^{-\alpha}+\epsilon_t e^{\alpha}$ to $dL_t/d\alpha=0 \Rightarrow \alpha_t=\tfrac12\ln\frac{1-\epsilon_t}{\epsilon_t}$, stating explicitly that $y_i,h_t(x_i)\in\{-1,+1\}$ (the fact that makes $y_ih_t(x_i)$ a correct/incorrect sign switch) and restoring the normalizer $Z_t$ that was missing from the original weight-update formula. — the original deck showed the alpha/weight-update formula with no derivation and no normalizer, which the task brief flagged by name as the kind of skipped derivation to fix.
- 2026-07-27 [content] Added a worked numeric table to "AdaBoost Reweights Examples" plugging ε=0.01→α≈2.30, ε=0.3→α≈0.42, ε=0.5→α=0 into the derived formula, matching the original note's unfulfilled instruction to "plug in errors near zero and near one-half."
- 2026-07-19 [content] Replaced the "Learning Rate and Number of Rounds" SVG, which reused the same generic S-curve chart as unrelated slides in other decks, with a computed train-error-vs-validation-error chart: training error decreases monotonically toward zero across n_estimators=20→400 while validation error decreases then rises past n_estimators≈100 (values derived directly to show the classic boosting overfitting U-shape, with a marked vertical line at the minimum). — the old chart's "primary signal"/"comparison" legend didn't correspond to anything the slide's cards discussed (small η / large η / "too many rounds can overfit"); the new chart is the actual evidence for that claim.
- 2026-07-27 [content] Removed the `assert np.isfinite(pred).all()` line from the "A Minimal Gradient-Boosting Loop" code slide as verification cruft not used by any sibling code slide in this module; confirmed the loop's logic (residual = y_reg − pred, shallow tree fit to residual, 0.1 learning-rate update) matches the equivalent check already run in `scripts/verify_presentation_examples.py::check_ensembles`.
- 2026-07-27 [content] Verified `AdaBoostClassifier(estimator=...)` (not `base_estimator=`) is correct for the installed sklearn 1.9.0 and matches `scripts/verify_presentation_examples.py`; added a speaker-note callout that `base_estimator=` was the pre-1.2 sklearn keyword, in case students run an older environment.
- 2026-07-27 [notes] Rewrote all 9 speaker-note blocks (8 original + 1 new slide) to textbook depth: defines weak learner, weighted error, function-space gradient descent, and the α/weight-update mechanics explicitly; adds the full worked AdaBoost derivation narration; explains why gradient boosting's base learner must stay shallow (a deep tree can zero out the residual in one round and invite immediate overfitting); contrasts boosting's active overfitting risk against bagging's "more trees rarely hurts" from the previous deck; ends with an explicit transition into "Why Ensembles Work."
- 2026-07-27 [verify] `slidev` dev server on port 3143, playwright-cli session `ens-audit`: `audit_slidev.js` result `{"total":10,"problems":[],"clickReveal":{"slide":2,"before":7,"after":6,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced `slides-export.pdf` with 10 pages, matching the 10-slide deck (was 9 slides/pages before this pass; first export attempt hit a transient Playwright/Vite dependency-reoptimization timeout and was simply rerun successfully).

### Why Ensembles Work

- 2026-07-27 [content] Inserted new slide "Majority Vote Beats Any Single Voter" (glowSeed 674) deriving the Condorcet-jury-style argument that B independent classifiers each correct with probability p>0.5, combined by majority vote, have combined accuracy $\sum_{k=\lceil B/2\rceil}^{B}\binom{B}{k}p^k(1-p)^{B-k}$, converging to 1 as B→∞; worked numeric example: p=0.7, B=3 gives 0.784, B=11 gives ≈0.94. — this is the classification-specific complement to the regression-only variance-of-an-average argument the deck already had; the task brief asked for worked numeric examples and this closes a real content gap (the deck previously had no argument that applies directly to classification voting).
- 2026-07-27 [content] Replaced the "Correlation Sets the Variance Floor" SVG, which was a single generic two-curve chart mislabeled "primary signal"/"comparison" and did not show three correlation levels at all, with three curves computed directly from $\rho+(1-\rho)/B$ at ρ=0/0.2/0.5 across B=1,10,50,100,500 — each curve now visibly flattens at its own correct asymptote (≈0, ≈0.2σ², ≈0.5σ²), matching the three cards' claims exactly. — flagged in an advisor review as the highest-value fix in the module: the previous chart contradicted its own caption and `audit_slidev.js` cannot detect a chart depicting the wrong math.
- 2026-07-27 [content] Added a worked numeric example to the "Variance of an Averaged Ensemble" card (σ²=4, ρ=0.2, B=50 → variance 0.864, compared against B=500 → 0.8064, showing the plateau is already nearly reached at B=50).
- 2026-07-27 [notes] Rewrote all 8 speaker-note blocks (7 original + 1 new slide) to textbook depth: derives the full ρσ²+(1-ρ)σ²/B formula from the Var(sum) covariance expansion rather than asserting it, walks the Condorcet majority-vote derivation and its independence-assumption caveat, ties bagging and boosting to opposite ends of the bias-variance tradeoff explicitly on the "Bias–Variance, Revisited" slide, and closes with an explicit transition into Unsupervised Learning.
- 2026-07-27 [verify] `slidev` dev server on port 3143, playwright-cli session `ens-audit`: `audit_slidev.js` result `{"total":8,"problems":[],"clickReveal":{"slide":2,"before":3,"after":2,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced `slides-export.pdf` with 8 pages, matching the 8-slide deck (was 7 slides/pages before this pass).

## Mathematical Foundations

### Calculus for Optimization

- 2026-07-27 [scaffold] Removed private 522MB node_modules, package-lock.json, local components/ copies (BarRow.vue/BorderGlow.vue, unused by slides.md), stray `global-bottom 2.vue` backup, and leftover `.claude/`, `.playwright/`, `.playwright-cli/` debris dirs. Replaced package.json (name now `calculus-for-optimization`, scripts point at `../../../slidev_template/node_modules/.bin/slidev`), style.css, uno.config.ts, and setup/main.ts with thin re-exports of the shared template; added vite.config.ts — now matches the standard scaffold pattern used by every other deck. Verified `npm run build` succeeds. — deck predated the shared-template convention and had drifted from `create_presentation.md` §6.
- 2026-07-27 [content] Split "Critical Points and Second Derivatives" into two slides: the original packed 3 cards + 2 display equations + an SVG cubic plot + a saddle-point image + caption into one slide, and the saddle-point image/caption were rendering entirely below the visible canvas (confirmed via Playwright screenshot before the fix; cannot determine whether this predates the re-scaffold since the old style.css no longer exists to compare against). New slide "Critical Points — Saddle Points" holds the image and explanation on its own, with the matching half of the original speaker notes moved over and glowSeed set to 174 (171/172/173 already used by neighboring slides). Deck slide count is now 25 (was 24). — a genuinely overcrowded slide with content invisible to students, not a style nitpick.
- 2026-07-27 [content] Removed a stray `<style>` block and `.compact-card` custom CSS class from the "Derivatives — Definition and Intuition" slide (the deck's only use of a `<style>` tag or bespoke class — confirmed via `grep -c '<style'` returning 0 across `slidev_template/slides.md` and every checked Core ML Concepts deck) and replaced it with the standard `[&>*]:my-0`/`leading-snug` utility-class pattern used identically elsewhere in this same deck. Removing the custom CSS initially caused the card column to overflow the slide by ~50px (denser default line-height); compensated with tighter `min-h-10`/`py-1.5`/`gap-1.5` on the four small cards, re-verified overflow-free by rerunning the audit and a manual screenshot. — pre-template drift the task explicitly asked to hunt for; custom `<style>` blocks bypass the shared UnoCSS/template styling system.
- 2026-07-27 [notes] Expanded the "The Chain Rule" speaker note (24 words, the shortest primary-content note in the deck against siblings running 75-118 words) with the outer/inner labeling caveat and an explicit forward pointer to backpropagation-as-repeated-composition. — noticeably thinner than every sibling note on an otherwise pivotal slide.
- 2026-07-27 [verify] `slidev` dev server on port 3144, playwright-cli session `math-audit`: final `audit_slidev.js` result `{"title":"Calculus for Optimization - Slidev","total":25,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. Manually screenshotted the two split slides (16, 17) and the edited card slide (2) to confirm no visual regressions beyond what the automated checker covers. `npm run build` succeeded against the re-scaffolded shared `slidev_template/node_modules`. `npm run export` was flaky on a cold Vite dependency cache — the first two attempts printed `✓ exported` while silently writing a broken 6.1KB/0-readable-page PDF (console showed `Failed to fetch dynamically imported module: .../print.vue` after a "Re-optimizing dependencies" message); a third attempt with a warm cache produced a healthy 11MB, 25-page PDF matching the 25-slide deck. Anyone exporting this deck cold should verify the resulting PDF's page count/size rather than trusting the "exported" message alone.

### Linear Algebra

- 2026-07-27 [scaffold] Same re-scaffolding as Calculus for Optimization: removed private 522MB node_modules, package-lock.json, local components/ copies, stray `global-bottom 2.vue` backup; replaced package.json (name `linear-algebra`), style.css, uno.config.ts, setup/main.ts with thin re-exports; added vite.config.ts. Verified `npm run build` succeeds. — deck predated the shared-template convention.
- 2026-07-27 [content] No content gaps or non-standard component/class usage found (`grep -c '<style'` and `<[A-Z]` component-tag scan both clean); all LaTeX and card markup already conforms to STYLE_GUIDE. No changes made — deck was already at the quality bar.
- 2026-07-27 [verify] `slidev` dev server on port 3144, playwright-cli session `math-audit`: `audit_slidev.js` result `{"title":"Linear Algebra for Machine Learning - Slidev","total":14,"problems":[],"clickReveal":{"slide":2,"before":5,"after":4,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded against the re-scaffolded shared `slidev_template/node_modules`; `npm run export` produced a healthy 5.7MB `slides-export.pdf` with 14 pages, matching the 14-slide deck.

### Probability and Stats

- 2026-07-27 [scaffold] Same re-scaffolding as the other two Mathematical Foundations decks: removed private 522MB node_modules, package-lock.json, local components/ copies, stray `global-bottom 2.vue` backup, and leftover `.playwright-cli/` debris; replaced package.json (name `probability-and-stats`), style.css, uno.config.ts, setup/main.ts with thin re-exports; added vite.config.ts. Verified `npm run build` succeeds. — deck predated the shared-template convention.
- 2026-07-27 [notes] Expanded the "Probability Distributions" speaker note (29 words, thinnest primary-content note in the deck against siblings running 40-67 words) with the PMF/PDF-height-isn't-a-probability misconception and the mu/sigma parameter-role callout. — noticeably thinner than sibling notes.
- 2026-07-27 [content] No slide-content gaps or non-standard component/class usage found beyond the notes tweak above (`grep -c '<style'` and `<[A-Z]` component-tag scan both clean); LaTeX and card markup already conform to STYLE_GUIDE.
- 2026-07-27 [verify] `slidev` dev server on port 3144, playwright-cli session `math-audit`: `audit_slidev.js` result `{"title":"Probability and Statistics for Machine Learning - Slidev","total":14,"problems":[],"clickReveal":{"slide":2,"before":5,"after":4,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded against the re-scaffolded shared `slidev_template/node_modules`; `npm run export` produced a healthy 6.3MB `slides-export.pdf` with 14 pages, matching the 14-slide deck.

## Model Evaluation

### Accuracy, Precision, Recall, F1, ROC-AUC

- 2026-07-28 [content] Reviewed in full: content and all 11 speaker-note blocks were already at textbook depth (150-350+ words/block, every formula derived symbol-by-symbol, worked numeric example reused across the deck, explicit accuracy-paradox and imbalanced-AUC misconceptions covered) from an earlier, uncommitted pass. No further content/notes changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3162, playwright-cli session `modeleval-audit`: `audit_slidev.js` result `{"total":11,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 4.5MB `slides-export.pdf`.

### Common Pitfalls - Data Leakage and Class Imbalance

- 2026-07-28 [content] Reviewed in full: content and all 10 speaker-note blocks were already at textbook depth from an earlier, uncommitted pass (leakage flow diagrams, a controlled noise-data demonstration showing leaky vs. honest cross-validation accuracy, target/temporal leakage, and the empirical-risk connection between class imbalance and training-time bias). No further content/notes changes needed.
- 2026-07-28 [content] Fixed a real rendering bug on "How Much Does Leakage Actually Inflate a Score?": a fenced ```python code block was placed directly adjacent (no blank line) to its enclosing `<div>`, which broke Vue's markdown-in-html parser and made the slide fail to load entirely (`Failed to fetch dynamically imported module`, `Element is missing end tag`). Fixed by adding blank lines around the fence per the STYLE_GUIDE rule on raw HTML containers. — why: this predated the notes pass and was invisible until the slide was actually loaded in a browser.
- 2026-07-28 [verify] `slidev` dev server port 3163, playwright-cli session `modeleval-audit`: first run failed (`passed:false`, console errors from the slide-5 parse failure above); after the fix, `audit_slidev.js` result `{"total":10,"problems":[],"clickReveal":{"slide":2,"before":7,"after":6,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 4.1MB `slides-export.pdf`.

### Confusion Matrices

- 2026-07-28 [content] Reviewed in full: content and all 7 speaker-note blocks were already at textbook depth from an earlier, uncommitted pass (axis-convention discipline, a same-accuracy-opposite-failure worked comparison, multiclass per-class precision/recall derivation, and a worked macro/weighted/micro averaging example exposing a rare-class failure hidden by weighted/micro recall). No further content/notes changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3161, playwright-cli session `modeleval-audit`: `audit_slidev.js` result `{"total":7,"problems":[],"clickReveal":{"slide":2,"before":3,"after":2,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 3.1MB `slides-export.pdf`.

### Statistical Significance in Model Comparison

- 2026-07-28 [content] Reviewed in full: content and all 9 speaker-note blocks were already at textbook depth from an earlier, uncommitted pass (sampling-distribution framing of a test score, the null-hypothesis frame with explicit p-value misconceptions, a fully worked McNemar's-test numeric example cross-checked against the exact binomial test, paired t-tests across CV folds with an independence caveat, and an explicit statistical-vs-practical-significance distinction). No further content/notes changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3164, playwright-cli session `modeleval-audit`: `audit_slidev.js` result `{"total":9,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 3.9MB `slides-export.pdf`.

## Neural Networks and Deep Learning Basics

### Activation Functions and Initialization

- 2026-07-27 [content] Expanded the deck from 9 to 15 slides. Added "Why Nonlinearity Matters" (algebraic proof that stacked linear layers collapse to one linear layer), "The Vanishing Gradient Chain, Numerically" (explicit 0.25¹…0.25⁵≈0.001 cascade tied to the chain rule), and two worked-example slides that compute an actual forward pass and backward pass by hand through a tiny 2-input/2-hidden-sigmoid/1-output network (real z, h, ŷ, BCE loss, and every weight gradient down to ∂L/∂z₁ ≈ −0.061, explicitly showing the ≈0.238 sigmoid-derivative shrink factor). Split the old "Zero Initialization" slide into "Naive Initialization Fails Two Ways" (adds the too-large/too-small random-init failure modes, not just the symmetry problem) followed by a new derivation slide "Why Preserve Variance Across Layers" that derives Var(w)=1/n from Var(y)=n·Var(w)·Var(x) rather than asserting Xavier/He formulas outright. Added a closing "Thank You" slide (the deck previously had none). Corrected the final "Next" pointer from the old generic "CNNs and RNNs" claim to "Backpropagation," matching the actual course order (Perceptrons → Activation Functions and Initialization → Backpropagation) confirmed against the Perceptrons deck's own next-topic slide. — why: the original 9-slide deck asserted vanishing gradients and Xavier/He scaling as facts without deriving either, and had no hand-computed numeric example anywhere in the Neural Networks module.
- 2026-07-27 [notes] Rewrote all 15 speaker-note blocks from ~10-11 words/block to textbook depth (~90-180 words/block), matching the Linear Algebra deck's standard: every term is defined on first use (saturation, fan-in/fan-out, symmetry breaking, BCE), every formula shown on the slide is walked step by step (including the σ(z)(1-σ(z)) maximization at z=0, the variance-preservation algebra, and every line of the two worked-example slides), a common student misconception is flagged per slide (e.g. "sigmoid saturates" vs. "sigmoid's derivative saturates"; ReLU only fixes the positive side; Leaky ReLU reduces but does not eliminate shrinkage), and each block ends with an explicit transition naming the next slide's idea. — why: original notes were one-sentence fragments a student could not learn from independently.
- 2026-07-27 [verify] Ran `npm run build` (succeeded). Started the dev server on port 3151 and audited via playwright-cli (session `afi-audit`, 1920×1080, `?clicks=99`, `scripts/audit_slidev.js`): `{"total":15,"problems":[],"clickReveal":{"slide":2,"before":6,"after":5,"passed":true},"consoleErrors":[],"passed":true}` — zero overflow/KaTeX/console errors across all 15 slides, click-reveal confirmed. Ran `npm run export`; `slides-export.pdf` has 15 pages, matching the 15-slide deck.

### Backpropagation

- 2026-07-28 [content] Reviewed in full: content and all 18 speaker-note blocks were already at textbook depth from an earlier, uncommitted pass — a complete hand-worked forward and backward pass through a concrete 2-input/2-hidden/2-output sigmoid network (every intermediate z, a, δ, and weight gradient computed numerically), a derivation of the single- and multi-variable chain rule, a forward-vs-reverse-mode autodiff efficiency argument, a PyTorch `loss.backward()` reproduction of the hand-derived gradients, and an explicit common-misconceptions slide. No further content/notes changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3170, playwright-cli session `nn-audit`: `audit_slidev.js` result `{"total":18,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 7.8MB `slides-export.pdf`.

### CNNs and RNNs (Light Touch)

- 2026-07-28 [content] Reviewed in full: the deck's first 6 slides and speaker notes were already at textbook depth from an earlier, uncommitted pass (parameter-explosion arithmetic for dense-on-images, the convolution cross-correlation formula, a compact CNN shape-trace, RNN weight-sharing-across-time). Left them unchanged.
- 2026-07-28 [notes] The final 3 slides ("Long Sequences Recreate Vanishing Gradients," "Choose Architecture From Data Structure," "Same Machinery, Specialized Layout") had one-sentence placeholder notes (~10-15 words) inconsistent with the rest of this same deck's depth. Rewrote all three to textbook-depth prose: ties the RNN vanishing-gradient problem explicitly to backpropagation-through-time and the Practical Training Issues deck's depth-based mechanism; turns the architecture-choice slide into an explicit decision rule (tabular→dense, grid-local→CNN, ordered-sequence→RNN/LSTM, with an honest Transformer caveat); closes by naming that training mechanics are unchanged across architectures and pointing to the two companion decks (Convolutional Neural Networks, RNNs and LSTMs) for full derivations. — why: this is a deliberately light-touch survey deck, but its own quality bar (set by its first 6 slides) was not met by its closing slides.
- 2026-07-28 [verify] `slidev` dev server port 3173, playwright-cli session `nn-audit`: `audit_slidev.js` result `{"total":8,"problems":[],"clickReveal":{"slide":2,"before":3,"after":2,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 3.1MB `slides-export.pdf`.

### Perceptrons and Multilayer Networks

- 2026-07-28 [content] Reviewed in full: content and all 19 speaker-note blocks were already at textbook depth from an earlier, uncommitted pass — McCulloch-Pitts/Rosenblatt/Minsky-Papert history, a fully hand-traced perceptron-learning-rule convergence on an AND gate (5 epochs, verified against a runnable NumPy loop), a rigorous convex-hull proof of why no line solves XOR, concrete numeric weights that solve XOR via a hidden layer with a full forward-pass verification table, an algebraic proof that linear layers without activations collapse to one linear map, and the Universal Approximation Theorem stated with its trainability/generalization caveats. No further content/notes changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3171, playwright-cli session `nn-audit`: `audit_slidev.js` result `{"total":19,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 7.5MB `slides-export.pdf`.

### Practical Training Issues - Vanishing Gradients, Batch Norm, Dropout

- 2026-07-28 [content] Reviewed in full: content and all 16 speaker-note blocks were already at textbook depth from an earlier, uncommitted pass — a derivation of vanishing/exploding gradients as a product of per-layer chain-rule factors, a numeric 0.9^L/1.1^L table plus the sigmoid-derivative-ceiling (0.25^20) calculation, four concrete mitigations (init, activation choice, residual connections, gradient clipping), a fully worked batch-norm numeric example (batch [2,4,4,6] normalized and rescaled by hand), the batch-norm train-vs-inference running-statistics distinction with its common `model.eval()` bug, a worked inverted-dropout numeric example with the unbiasedness derivation, and the regularization-vs-ensembling dual explanation for dropout. No further content/notes changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3172, playwright-cli session `nn-audit`: `audit_slidev.js` result `{"total":16,"problems":[],"clickReveal":{"slide":2,"before":6,"after":5,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 6.4MB `slides-export.pdf` (export printed a benign `<tr>`-without-`<tbody>` HTML warning on the numeric-table slide — cosmetic only, no rendering or overflow effect per the Playwright audit).

## Optimization in Practice

### Gradient Descent Variants - SGD, Momentum, Adam

- 2026-07-27 [content] Added a new "Nesterov Momentum Looks Ahead First" slide with the look-ahead update equations, distinguishing it from classical/heavy-ball momentum — the deck previously named-dropped momentum with no mention of Nesterov at all. — task explicitly calls out momentum-vs-Nesterov as a required misconception to cover.
- 2026-07-27 [content] Added a new "Four Optimizers, One Loss Surface" slide with an inline SVG showing SGD, momentum, RMSProp, and Adam trajectories on the same elliptical contour plot, plus a 4-card legend. — task requires a worked comparison of optimizer behavior on a shared loss surface with an SVG of the different paths; none existed before.
- 2026-07-27 [content] Fixed slide-9 overflow from the new comparison SVG (bottom overflowed the frame by ~29-217px per Playwright audit) by shrinking the viewBox, max-width (3xl→2xl), and legend card padding/text size. — first audit run failed with `passed:false` due to this overflow.
- 2026-07-27 [notes] Rewrote all 11 speaker-note blocks from ~11-word fragments to textbook-depth paragraphs (roughly 1000-1800 characters each): every symbol in every update equation (θ, α, g_t, β, β₁, β₂, v_t, m_t, ε, bias-correction terms) is now defined explicitly, each formula is walked term-by-term, and each slide ends with an explicit transition to the next. — matches target depth demonstrated in Mathematical Foundations/Presentations/Linear Algebra.
- 2026-07-27 [notes] Added explicit misconception callouts: SGD notes clarify that shrinking the learning rate reduces oscillation but does not fix an ill-conditioned surface; momentum notes flag that "momentum" and "Nesterov momentum" are not interchangeable; Adam-comparison notes state plainly that Adam is not proven to generalize better than tuned SGD+momentum (with the ResNet/ImageNet counterexample) and that the loss-surface SVG is a stylized convex illustration, not a claim about real non-convex surfaces. — required by task instructions to flag common misconceptions.
- 2026-07-27 [verify] Playwright audit (`scripts/audit_slidev.js`) passed on all 11 slides after the slide-9 fix: `{"total":11,"problems":[],"clickReveal":{"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` and `npm run export` succeeded; `slides-export.pdf` has 11 pages, matching the 11 slides.

### Hyperparameter Tuning Strategies

- 2026-07-27 [content] Added an inline SVG to "Random Search Covers Important Axes Better" contrasting a 3×3 grid (only 3 distinct values on the important axis) against 9 random draws (9 distinct values), making the Bergstra & Bengio argument concrete instead of only asserted in card text. — the deck previously stated the claim with no supporting visual.
- 2026-07-27 [notes] Rewrote all 9 speaker-note blocks from ~11-word fragments to textbook-depth paragraphs: derives N = v^h with a worked example (3^10 = 59,049), walks the grid-search fold-multiplier cost (16 configs × 5 folds = 80 fits), explains log-uniform sampling with a concrete learning-rate example, walks the RandomizedSearchCV code line by line (`loguniform`, `n_iter`, `random_state`), explains the Bayesian-optimization surrogate/acquisition loop and when it is/isn't worth the overhead versus random search, and spells out the broad-to-narrow workflow's train/validation/test discipline. — matches target depth demonstrated in Mathematical Foundations/Presentations/Linear Algebra.
- 2026-07-27 [notes] Added explicit tradeoff and misconception content: grid search's exhaustiveness-vs-waste tradeoff with concrete fit counts; random search's "won't find the exact grid optimum but explores wider ranges per axis" clarification; Bayesian optimization's sequential/hard-to-parallelize cost versus random search's embarrassing parallelism; and the discipline point that "peeking" at test performance to pick among a few final configurations silently biases the reported test accuracy. — task requires real tradeoffs, not just a list of method names.
- 2026-07-27 [verify] Playwright audit passed on all 9 slides: `{"total":9,"problems":[],"clickReveal":{"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` and `npm run export` succeeded; `slides-export.pdf` has 9 pages, matching the 9 slides.

## Supervised Learning - Classification

### Decision Trees

- 2026-07-28 [content] Reviewed in full: content and all 9 speaker-note blocks were already at textbook depth — a derivation of Gini/entropy impurity, two worked information-gain examples (a perfect split and a messier 6/4 split with full arithmetic), an axis-aligned-partition geometric framing, a bias-variance depth comparison, cost-complexity pruning math, and regression trees. No further changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3180, playwright-cli session `svc-audit`: `audit_slidev.js` result `{"total":9,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 4.1MB `slides-export.pdf`.

### k-Nearest Neighbors

- 2026-07-28 [content] Reviewed in full: content and all 10 speaker-note blocks were already at textbook depth — a worked hand-traced vote (Euclidean distances computed for 4 points, k=3 majority), the Minkowski distance family with unit-ball geometry, a live curse-of-dimensionality simulation, and an explicit feature-scaling warning with a leakage-safe pipeline. No further changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3181, playwright-cli session `svc-audit`: `audit_slidev.js` result `{"total":10,"problems":[],"clickReveal":{"slide":2,"before":5,"after":4,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 4.1MB `slides-export.pdf`.

### Logistic Regression

- 2026-07-28 [content] Reviewed in full: content and all 11 speaker-note blocks were already at textbook depth — a full maximum-likelihood derivation of log loss from a Bernoulli label model, a three-step chain-rule derivation of the gradient collapsing to (p̂−y)x, a numerical finite-difference verification against the closed-form gradient, the C=1/λ inverse-regularization-naming gotcha, and softmax shown as the K-class generalization reducing to sigmoid at K=2. No further changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3182, playwright-cli session `svc-audit`: `audit_slidev.js` result `{"total":11,"problems":[],"clickReveal":{"slide":2,"before":5,"after":4,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 5.1MB `slides-export.pdf`.

### Naive Bayes

- 2026-07-28 [content] Reviewed in full: content and all 8 speaker-note blocks were already at textbook depth — the classification form of Bayes' theorem with all four named terms, a fully worked spam-classification posterior computed by hand (score_spam=.168, score_ham=.012, normalized to ≈.933), the conditional-independence assumption with an honest "New York" counterexample, Gaussian NB's MLE-based closed-form fitting, Laplace smoothing derived from the zero-probability-collapse failure mode, and a generative-vs-discriminative comparison table citing Ng & Jordan 2001. No further changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3183, playwright-cli session `svc-audit`: `audit_slidev.js` result `{"total":8,"problems":[],"clickReveal":{"slide":2,"before":5,"after":4,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 3.5MB `slides-export.pdf`.

### Support Vector Machines and the Kernel Trick

- 2026-07-28 [content] Reviewed in full: content and all 10 speaker-note blocks were already at textbook depth — a point-to-hyperplane derivation of the 2/‖w‖ margin formula, a fully worked margin calculation on two concrete support vectors verified by a runnable NumPy assertion, the soft-margin/slack-variable formulation with the C-direction gotcha, hinge loss connected explicitly to the general ERM template with a sparsity argument, a full Lagrangian-duality derivation of the dual problem, and the kernel trick motivated via a concentric-circles lift. No further content/notes changes needed.
- 2026-07-28 [content] Fixed a real code-block overflow on "A Worked Margin Calculation": an inline comment on the `assert margin_width == 2.0` line was too long and pushed the code block past the bottom of the canvas. Shortened the comment.
- 2026-07-28 [verify] `slidev` dev server port 3184, playwright-cli session `svc-audit`: first run failed (`passed:false`, overflow on slide 4 from the comment above); after the fix, `audit_slidev.js` result `{"total":10,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 4.7MB `slides-export.pdf`.

## Supervised Learning - Regression

### Linear Regression - Closed-Form and Gradient Descent

- 2026-07-28 [content] Reviewed in full: content and all 14 speaker-note blocks were already at textbook depth — a full expand-and-differentiate derivation of the normal equations (including the y^T Xw = w^T X^T y scalar-transpose trick), a worked 3-point hand example cross-checked against `np.linalg.lstsq`, an explicit warning against explicit matrix inversion in code, the rank-deficiency/singularity failure mode with a redundant-feature SVG, the gradient-descent zig-zag-on-elliptical-contours picture, and a closed-form-vs-gradient-descent cost comparison (O(nd²+d³) vs O(nd) per step). No further changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3190, playwright-cli session `svr-audit`: `audit_slidev.js` result `{"total":15,"problems":[],"clickReveal":{"slide":2,"before":6,"after":5,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 6.1MB `slides-export.pdf`.

### Polynomial Regression and Regularization

- 2026-07-28 [content] Reviewed in full: content and all 13 speaker-note blocks were already at textbook depth — polynomial features framed correctly as "linear in parameters, nonlinear in inputs," a full ridge derivation reusing the linear-regression normal-equation identities, an eigenvalue-shift proof of why ridge restores invertibility, a shrinkage-factor derivation of the ridge bias-variance tradeoff, a from-scratch soft-thresholding derivation of lasso's sparsity under an orthonormal-design assumption, and the geometric L1-diamond-vs-L2-circle constraint-region argument as an independent second proof of the same sparsity result. No further changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3191, playwright-cli session `svr-audit`: `audit_slidev.js` result `{"total":14,"problems":[],"clickReveal":{"slide":2,"before":5,"after":4,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 6.1MB `slides-export.pdf`.

## Unsupervised Learning

### Dimensionality Reduction - PCA, t-SNE, UMAP

- 2026-07-28 [content] Reviewed in full: the deck's first 6 slides (motivation, the maximize-projected-variance objective, a full Lagrangian derivation showing the optimal direction must be an eigenvector of Σ, the full eigendecomposition change-of-basis, the SVD route scikit-learn actually uses, and a worked 2-feature PCA-by-hand example with real eigenvalues) were already at textbook depth from an earlier, uncommitted pass. Left unchanged.
- 2026-07-28 [notes] The remaining 4 slides ("PCA in scikit-learn," "Explained Variance Chooses k," "PCA vs. t-SNE / UMAP," and the closing slide) had one-sentence placeholder notes (~8-10 words) despite the deck's own intro promising t-SNE/UMAP would be "explained at the level of what problem they solve and how" — that promise was never delivered in the original notes. Rewrote all four: the scikit-learn slide now covers the `n_components=0.95` API and the leakage discipline of fitting only on training data; the explained-variance slide draws the explicit elbow-method parallel to k-means; the t-SNE/UMAP slide now explains the actual problem being solved (neighbor-preservation on curved manifolds PCA's linear subspace cannot represent) and gives three specific, concrete cautions (inter-cluster distance, apparent size, and stochasticity are all not meaningful) rather than a vague warning; the closing slide ties PCA/GMM/k-means together via the shared covariance-matrix object and transitions explicitly into the Neural Networks module.
- 2026-07-28 [content] Fixed two generic placeholder SVG charts (labeled "primary signal"/"comparison," not tied to any real data — the same category of bug flagged and fixed in Ensemble Methods and Optimization in Practice) on "Explained Variance Chooses k": replaced with an actual scree plot showing per-component variance bars dropping off sharply alongside a cumulative-variance curve leveling off near PC4, matching what the slide's own notes now describe.
- 2026-07-28 [verify] `slidev` dev server port 3202, playwright-cli session `unsup-audit`: `audit_slidev.js` result `{"total":11,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 4.6MB `slides-export.pdf`.

### Gaussian Mixture Models

- 2026-07-28 [content] This deck was substantially thinner than its Unsupervised Learning siblings: most speaker notes were one-line placeholders ("Introduce the topic...", "Build the one-dimensional picture...", "Place EM beside k-means assign/update...") rather than actual explanations. Rewrote all 8 original slides' notes to textbook depth, deriving the generative mixture story, the Bayes'-theorem responsibility formula (explicitly contrasted with k-means' hard assignment), the E-step/M-step update equations (with the M-step shown to collapse to k-means' update in the hard-assignment limit), the `covariance_type` modeling choice and its parameter-count cost, and the AIC/BIC model-selection formulas.
- 2026-07-28 [content] Added a new worked slide, "A Worked Responsibility Calculation," computing a real Gaussian-mixture posterior by hand for a single point under two 1D components (γ≈0.880/0.120), verified against a runnable `scipy.stats.norm`-based script — the deck previously had no numeric example at all, unlike every sibling deck in this module.
- 2026-07-28 [content] Fixed a generic placeholder SVG chart (labeled "primary signal"/"comparison") on "Choose Components With Information Criteria" — replaced with a chart showing log-likelihood rising monotonically alongside BIC forming a genuine U-shape with a minimum at K=3, matching the slide's own notes.
- 2026-07-28 [notes] Rewrote the closing slide's notes to make the promised k-means-as-a-limiting-case-of-GMM connection fully explicit and derived (equal weights, shared spherical covariance, σ²→0 collapses soft responsibilities to hard assignment and the M-step mean to k-means' plain average), and added an explicit transition into the Dimensionality Reduction deck.
- 2026-07-28 [verify] `slidev` dev server port 3201, playwright-cli session `unsup-audit`: `audit_slidev.js` result `{"total":9,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}` (deck grew from 8 to 9 slides with the new worked example). `npm run build` succeeded; `npm run export` produced a 3.5MB `slides-export.pdf`.

### k-Means and Hierarchical Clustering

- 2026-07-28 [content] Reviewed in full: content and all 12 speaker-note blocks were already at textbook depth — a derivation of why the mean minimizes within-cluster sum of squares (with the TSS=WSS+BSS identity), Lloyd's algorithm's monotone-convergence argument, a fully worked 6-point/K=2 iteration with real assign/update arithmetic, k-means++ mechanics, the silhouette-score formula, an explicit "k-means assumes spherical equal-size clusters" failure-mode catalog, and a dendrogram/linkage explanation (single/complete/average/Ward). No further changes needed.
- 2026-07-28 [verify] `slidev` dev server port 3200, playwright-cli session `unsup-audit`: `audit_slidev.js` result `{"total":12,"problems":[],"clickReveal":{"slide":2,"before":4,"after":3,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 5.3MB `slides-export.pdf`.

## New Decks

### Convolutional Neural Networks

- 2026-07-28 [content] Content and all 10 speaker-note blocks were already at textbook depth from an earlier, uncommitted authoring pass (a problem-first framing: dense-layer parameter explosion → convolution built from scratch with a worked Sobel-kernel numeric example → weight-sharing parameter-count comparison (150.5M vs. 896) → the Zeiler & Fergus 2014 feature hierarchy with citation → a worked max-pooling numeric example → a full architecture shape-trace ending in a VGG16 real-world reference point → a closing recap mapping all four opening problems to their fixes). This deck had never been verified or logged before this pass.
- 2026-07-28 [content] Fixed a real rendering bug found during first verification: 9 SVG `text`/`g` elements across 4 slides used bare `font-size`/`font-weight` presentation attributes instead of `style="font-size:...px"` — the same bug class previously found and fixed in the RNNs and LSTMs deck, where a higher-specificity global stylesheet rule overrides bare SVG presentation attributes, rendering labels at native/oversized scale and blowing 3 slides ("The Convolution Operation," "Convolution, Worked in Numbers," "The Full CNN Architecture") out of the visible canvas. Converted all 9 to inline `style` attributes.
- 2026-07-28 [content] Fixed a genuine content-density overflow on "Max Pooling — A Worked Example": two stacked 3-column card grids (the worked-example row plus a three-benefit row) exceeded the canvas height. Tightened card padding/font-size/line-height and shrunk the input-grid SVG's max-width; re-verified overflow-free.
- 2026-07-28 [verify] `slidev` dev server port 3174, playwright-cli session `nn-audit`: first run failed (`passed:false`, overflow on slides 3, 4, 7, 8 from the two bugs above); after both fixes, `audit_slidev.js` result `{"total":10,"problems":[],"clickReveal":{"slide":2,"before":3,"after":2,"passed":true},"consoleErrors":[],"passed":true}`. `npm run build` succeeded; `npm run export` produced a 4.2MB `slides-export.pdf` matching the 10-slide deck.

### RNNs and LSTMs

- 2026-07-27 [content] Authored a brand-new 14-slide deck from scratch (scaffold pre-existed): title; why feedforward/MLP nets fail on sequences (order-blindness + fixed input size); tokenization (word/subword/character); embeddings as learned lookup vectors feeding x_t; "what is an RNN" with the h_t = f(W_xh x_t + W_hh h_{t-1} + b) recurrence; an unrolled-in-time SVG chain diagram; BPTT derivation of ∂h_T/∂h_k as a product of T-k Jacobians ≈ W_hh^(T-k); an eigenvalue-based vanishing/exploding gradient slide with a numeric 0.5^n vs 1.5^n table (n=10,20,50) plus the telephone-game analogy; a full hand-coded SVG anatomy of one LSTM cell (forget/input/output gates, cell-state highway); two equation slides deriving f_t, i_t, C̃_t, C_t, o_t, h_t explicitly; a slide on why the mostly-elementwise cell-state update mitigates (not eliminates) vanishing gradients, tied explicitly to the residual/skip-connection analogy; and a wrap-up comparing RNN vs. LSTM chains with a forward pointer to attention/Transformers, citing Hochreiter & Schmidhuber (1997) — why: source outline lived in a different repo/component system (BorderGlow Vue components, different SVG palette) and needed a full rewrite into this course's plain-HTML-card + inline-SVG + accent-palette conventions, combining RNNs and LSTMs into one deck per the confirmed scope decision.
- 2026-07-27 [notes] Wrote fresh, textbook-depth speaker notes for all 14 slides (not adapted from the source deck): every term defined on first use (hidden state vs. cell state, sigmoid-for-gating vs. tanh-for-candidate-value, Hadamard/elementwise product vs. matrix multiplication, Jacobian), each equation walked symbol-by-symbol, common misconceptions flagged explicitly (gates as separate neural networks vs. a [0,1] elementwise-gating vector; cell state and hidden state treated as the same object; LSTMs assumed to fully eliminate rather than substantially mitigate vanishing gradients; depth-in-the-unrolled-diagram misread as distinct-layers-with-distinct-weights), and every block ends with an explicit transition to the next slide.
- 2026-07-27 [verify] Fixed a `<g v-for="none">` leftover placeholder that broke the Vue/SVG compiler (`npm run build` failed with "v-for has invalid expression"); removed it. Converted 49 bare SVG presentation attributes (`font-size="N"`, `text-anchor="middle"`, `font-weight`, `font-style`) across all new inline SVGs into `style="font-size:Npx;..."` — bare attributes were being overridden by a higher-specificity global stylesheet rule, rendering text at native/huge size and blowing slides 3, 6, and 9 out of frame. Also shrank the LSTM-cell-anatomy SVG (`max-w-4xl`→`max-w-3xl`, tightened subtitle/margin) to fix a bottom overflow. Ran `slidev build` (succeeded), started a dev server on port 3411, and ran `scripts/audit_slidev.js` via playwright-cli (`-s=rnn-audit`, 1920×1080, `?clicks=99`) until it returned `{"total":14,"problems":[],"clickReveal":{"passed":true},"consoleErrors":[],"passed":true}`. Spot-checked screenshots of the title, unrolled-RNN, BPTT-derivation, eigenvalue-numeric, LSTM-cell-anatomy, and closing slides — no overflow, no KaTeX errors. `npm run build` and `npm run export` both succeeded; `slides-export.pdf` (5.4MB) has 14 pages, matching the 14-slide deck.

## Cleanup / Scaffolding

- 2026-07-27 [scaffold] Deleted `Mathematical Foundations/Presentations/test/` — a stray ~504MB duplicate draft of "Calculus for Optimization" (different directory literally named `test`, missing `setup/`, missing `glowSeed` frontmatter, used non-template `GlowBox`/`kicker` classes, no export PDF). Not a real course deck.
- 2026-07-27 [scaffold] Deleted `Mathematical Foundations/Presentations/.playwright-cli/` — leftover empty Playwright CLI session snapshot files from a prior verification run.
- 2026-07-27 [scaffold] Fixed `scripts/audit_all_decks.sh`: replaced its hardcoded 7-module `find` list (which omitted Core ML Concepts, Mathematical Foundations, Supervised Learning - Classification, Supervised Learning - Regression) with a course-root-relative `find . -path '*/node_modules' -prune -o -path '*/Presentations/*/slides.md' -print0`, so it now discovers all 34 existing decks plus any new decks (e.g. the CNN/RNN decks added in this audit) automatically.

## Verification Log

- 2026-07-29 [verify] Ran the fixed `scripts/audit_all_decks.sh` end-to-end across the entire course: it discovered and audited all 38 decks (34 original + Convolutional Neural Networks, RNNs and LSTMs, plus the 2 previously-untracked Mathematical Foundations/Core ML Concepts/Supervised Learning decks the old hardcoded module list had missed — now correctly included). Every deck returned `{"passed":true}` — zero overflow beyond the 3px tolerance, zero `.katex-error` elements, click-reveal confirmed working, and zero browser console errors on all 38 decks. Combined slide count across the full course: 452 slides. No deck required any further fix as a result of this sweep; every issue this sweep would have caught had already been found and fixed during each deck's own Phase 1/3 verification pass earlier in the audit.
- 2026-07-29 [verify] Confirmed `scripts/generate_missing_presentations.mjs` was not rerun at any point after any deck's `slides.md` became hand-edited, per the plan's explicit constraint — every deck in this course is now authoritative as its own `slides.md` file, with no risk of a generator rerun silently clobbering the content/notes work done in this audit.
